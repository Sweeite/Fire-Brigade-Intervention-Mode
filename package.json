{
  "name": "fire-brigade-intervention-mode",
  "private": true,
  "workspaces": [
    "engine",
    "web"
  ],
  "scripts": {
    "build:engine": "npm run build --workspace=engine",
    "test:engine": "npm test --workspace=engine",
    "dev:web": "npm run dev --workspace=web",
    "build:web": "npm run build --workspace=web"
  }
}
